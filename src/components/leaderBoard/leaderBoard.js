import React from 'react';

import CheckAuth from '../login/loginVerification';

const leaderBoard = () => (
    <CheckAuth>
        <div>
            Leader Board
        </div>
    </CheckAuth>
);

export default leaderBoard;